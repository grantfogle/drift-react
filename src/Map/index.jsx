import React, { Fragment } from "react";

import RiverMapSearch from "./riverMapSearch";

const MapContainer = () => {
  return (
    <Fragment>
      <RiverMapSearch />
      {/* 1. Search Input Autocomplete form */}
      {/* 2. Map Layer Background */}
      {/* 3. Map Layer background */}
      {/* 4. Tools */}
      {/* 5. Assorted Icons (weather, map type toggle, direction) */}
    </Fragment>
  );
};

export default MapContainer;
